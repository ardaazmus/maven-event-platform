---
description: "MavenForms canonical Event Platform source, packet workflow and security gates"
globs: "src/**/*,tests/**/*,prisma/**/*,scripts/**/*,docs/workflow/**/*"
alwaysApply: false
---

Kanonik ürün/faz kaynağı: `docs/MavenForms_Platform_Core_Master_Plan_2026-09-17/`. Önce `AGENTS.md`, `PROJECT_CONTEXT.md`, `STATUS.md` ve master-plan `sourceOfTruth` taşıyan READY packet’i oku. Legacy root dokümanları yeni işi yönetmez. Event-first model, server-side scope/auth/idempotency/public-private/audit ve `node scripts/workflow.mjs verify <packet>` zorunludur.
