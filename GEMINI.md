# MavenForms Gemini adapter

Project instructions are canonical in `AGENTS.md`. Read `PROJECT_CONTEXT.md`, `STATUS.md`, and the relevant READY packet under `docs/workflow/packets/`. Keep each change inside the packet, preserve the fixed phase order, and run `node scripts/workflow.mjs verify <packet>` with its required checks.
