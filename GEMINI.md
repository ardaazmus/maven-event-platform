# MavenForms Gemini adapter

Kanonik kaynak `docs/MavenForms_Platform_Core_Master_Plan_2026-09-17/` klasörüdür. `AGENTS.md`, `PROJECT_CONTEXT.md`, `STATUS.md` ve master-plan `sourceOfTruth` taşıyan READY packet okunur. Legacy root belgeleri yeni işi yönetmez. Değişiklik packet `allowedFiles` içinde kalır ve `node scripts/workflow.mjs verify <packet>` ile doğrulanır.
