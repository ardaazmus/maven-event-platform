# MavenForms — güncel durum

**Tarih:** 2026-09-07  
**Durum:** Geliştirme devam ediyor; release açık değil.  
**Aktif iş:** Context katmanı `CTX-01B`, P-12B ve P-12C-01..P-12C-10 yerel olarak geçti; P-12D-01..P-12D-02 ve R-00-01..R-09 yerel kapılardan geçti. `R-10A` dış kanıt teslim rehberi ve `R-10B` kanal ayrımı tamamlandı; `R-10` global release gate’i dış kanıtlar nedeniyle `NO-GO/BLOCKED` olmaya devam ediyor. `R-10C` ile bu kararın ilk company-owned/first-party local-staging pilot capability’si ayrıştırıldı; production, live mutation, SaaS tenant ödemesi ve release hâlâ kapalıdır. V4 öncesi aktif ara kapı `FORM-UX-00` ile ekran/özellik/bağlantı envanteridir.

## Sürümlü ürün yürütmesi — 2026-09-06

- `V1-00A`: **LOCAL_PASS**. V1 modül matrisi server-side immutable sözleşme olarak eklendi. Maven iç kullanım için kayıt/form, manuel ödeme durumu ve katılımcı bildirimi açık; online ödeme, fatura, Paraşüt, transactional invoice delivery ve SaaS kapalıdır. Hedef test, TypeScript, production build ve `/api/ready` geçti. Bu yerel kanıt production release onayı veya R-10 dış kanıtı değildir.
- `V1-01`: **LOCAL_PASS**. Manuel ödeme durumu `unpaid`, `paid`, `review`, `refunded` geçişleri role ve state graph ile sınırlandı; actor/submission/time audit metadata’sı üretiliyor. Manuel `paid`, provider doğrulaması olarak sunulmuyor. Hedef durum, submission regression, TypeScript, production build ve `/api/ready` geçti.
- `V1-02`: **LOCAL_PASS**. Hoş geldiniz ve ödeme bekliyor mesajları yalnız `notification` sınıfında üretildi; alıcı server snapshot’tan, ödeme metni yetkili mesaj ayarından türetiliyor. Fatura/transactional kanal bu fazda açılmadı. Hedef policy testi, TypeScript, production build ve `/api/ready` geçti.
- `V1-03`: **LOCAL_PASS**. Event registration, research survey ve quiz kullanım profilleri güvenli varsayılanlarla tanımlandı; bilinmeyen profil fail-closed, ödeme/fatura/yetki açma yok ve profil form ayarlarında version snapshot’tan bağımsız tutuluyor. Hedef profil testi, publish regression, TypeScript, production build ve `/api/ready` geçti.
- `V1-04A`: **LOCAL_PASS**. Seçili form ekranında canlı yayınlanan form, istatistik, yanıt arama/filtre ve yanıtlar sırası korunuyor; responsive tablo ve iframe sandbox sözleşmesi testle kilitlendi. Manuel ödeme işaretleme yalnız owner/admin/accounting için ayrı capability ile korunuyor, accounting genel yanıt durumunu değiştiremiyor ve tekrar tıklama engelleniyor. Online ödeme hâlâ kapalıdır.
- `V1-05`: **LOCAL_PASS**. V1 iç kullanım checklist’i kayıt, yayınlanmış public snapshot, yanıt listesi, manuel ödeme, izinli bildirim, public/private sınırı ve gelecek modüllerin kapalı olmasıyla doğrulandı. Bu kanıt production release approval değildir; global R-10 `NO-GO` korunuyor.
- `V2-00A`: **LOCAL_PASS**. Provider evidence matrisi iyzico hosted checkout, Stripe uygunluk ve Google Pay gateway/direct ayrımını kanıt alanlarıyla tanımlıyor. Bun/Kod yolu, TypeScript, production build ve `/api/ready` geçti; live ödeme ve public payment hâlâ kapalı, global R-10 `NO-GO` korunuyor.
- `V2-01`: **LOCAL_PASS**. First-party ödeme pilotu için server-owned workspace/merchant eşleşmesi ve local/staging scope gate sözleşmesi eklendi; browser scope override, SaaS scope ve production kanıtsız mutation reddediliyor. Hedef/regresyon testleri, TypeScript, production build ve `/api/ready` geçti; gerçek provider ve global R-10 kanıtı hâlâ yok.
- `V2-02`: **LOCAL_PASS**. İyzico hosted checkout, server-created PaymentOrder, workspace-scoped active connection çözümleme, server callback URL ve opaque checkout response ile bağlandı. Kart/secret public route’a taşınmıyor; yalnız test modu kullanılabiliyor, callback finansal başarı yazmıyor. Hedef checkout, route, persistence, TypeScript, production build ve `/api/ready` geçti; gerçek provider sandbox ve global R-10 kanıtı yok.
- `V2-03A`: **LOCAL_PASS**. Stripe/iyzico webhookları raw-body imza, active connection, bounded identity, duplicate inbox ve workspace/provider/PaymentOrder correlation kapılarıyla bağlandı; refund/chargeback otomatik fatura sonucu değildir. Hedef/regresyon testleri, TypeScript, production build, `/api/ready` ve workflow verify geçti. Canlı provider/refund kanıtı yok; global R-10 `NO-GO/BLOCKED` korunuyor.
- `V2-03B`: **LOCAL_PASS**. Provider retrieve sonucu güvenli/DB-uyumlu minor-unit sınırı, normalize response sözleşmesi, monotonic reducer ve terminal state fence ile doğrulandı; tutar sınırı dışı veya belirsiz yanıt finansal başarı üretmiyor. Hedef/regresyon testleri, TypeScript, production build, `/api/ready` ve workflow verify geçti. Canlı provider/retrieve kanıtı yok; global R-10 `NO-GO/BLOCKED` korunuyor.
- `V2-03C`: **LOCAL_PASS**. Retrieve timeout/rate-limit/invalid response failure’ları bounded retry veya terminal failure ile lease’i serbest bırakıyor; stale attempt failure kararı persistence mutation yapmıyor. Hedef/regresyon testleri, TypeScript, production build, `/api/ready` ve workflow verify geçti. Canlı provider/retrieve kanıtı yok; global R-10 `NO-GO/BLOCKED` korunuyor.
- `V2-04A`: **LOCAL_PASS**. Stripe conditional capability; eligibility, merchant sorumluluğu, ülke/para birimi ve live R-10 kanıtı olmadan provider gösterimi/mutation’ı fail-closed tutuyor. Public provider config allowlist ve Stripe checkout/retrieve/provider contract regresyonları, TypeScript, production build ve `/api/ready` geçti. Gerçek Stripe merchant/sandbox kanıtı yok; global R-10 `NO-GO/BLOCKED` korunuyor.
- `V2-04B`: **LOCAL_PASS**. Stripe hosted checkout contract’ı server-only credential, test/live mode, amount/currency, DB-uyumlu minor-unit, idempotency, HTTPS ve opaque response sınırlarıyla doğrulandı. Hedef/regresyon testleri, TypeScript, production build ve `/api/ready` geçti. Route wiring, gerçek Stripe merchant/sandbox ve live mutation yok; global R-10 `NO-GO/BLOCKED` korunuyor.
- `V2-04C`: **SUPERSEDED/BLOCKED**. Yeni provider karar kaydı packet begin sonrasında eklendiği için workflow bütünlük kapısı bu eski revision’ı kapattı; kod veya karar kaydı kaybedilmedi.
- `V2-04C-A`: **LOCAL_PASS**. İyzico Türkiye birincil first-party public payment policy’si server-side provider binding ile doğrulandı; public body provider/merchant/workspace/mode/amount/currency/callback seçemiyor, doğrulanmamış Stripe/banka Sanal POS public mutation’a giremiyor. Karar kaydı, bootstrap yönlendirmesi, TypeScript, ilgili regresyonlar, production build ve `/api/ready` geçti. Gerçek provider mutation ve global R-10 `NO-GO/BLOCKED` olarak kalıyor.
- `V2-04C-B`: **LOCAL_PASS**. V2-04C-A kararı ve iyzico-birincil binding receipt’i overwrite edilmeden yeniden doğrulandı; ilgili regresyonlar, TypeScript, production build ve `/api/ready` geçti. Gerçek provider mutation ve global R-10 durumu değişmedi.
- `V2-05`: **DEFERRED**. Google Pay direct, banka Sanal POS ve Stripe V3+ gibi gateway/provider kanıtına bağlı seçenekler resmi merchant/gateway/3DS/aktivasyon kanıtı gelmeden UI veya mutation kapsamına alınmadı.
- `V2-06`: **LOCAL_PASS**. Succeeded ödeme sonrasında yalnız server-owned allowlist snapshot ve `transactional` sınıfında “Faturanız hazırlanıyor” intent’i üretildi; belge linki/eki, issued/document-ready iddiası, Paraşüt ve otomatik/toplu gönderim açılmadı. Hedef ve fatura regresyonları, TypeScript, production build ve `/api/ready` geçti. Gerçek provider/e-posta teslimatı ve global R-10 `NO-GO/BLOCKED` korunuyor.
- `V2-07`: **LOCAL_PASS**. Muhasebecinin dış sistemde kestiği PDF/XML/XLSX belge yalnız authenticated invoice writer tarafından server-owned private/quarantine alanına alınıyor; dosya türü/MIME/boyut/hash ve arşiv riskleri doğrulanıyor. Fatura–ödeme eşleştirmesi yalnız stable reference ile yapılıyor, PII eşleştirme kanıtı sayılmıyor; her karar manuel approval gerektiriyor. `document_ready` yalnız issued + matched + approved + clean + quarantined koşullarının tamamında mümkün. Upload, approval ve ready route’larının actor/workspace scope’ları session/server tarafından bağlanıyor ve upload delivery başlatmıyor. Hedef manuel akış sözleşmesi ve ilgili regresyonlar, TypeScript, production build, `/api/ready` geçti. Gerçek muhasebeci belgesi, AV taraması, provider/e-posta teslimatı ve global R-10 `NO-GO/BLOCKED` korunuyor.
- `V2-08`: **LOCAL_PASS**. `document_ready` olmuş private/quarantined/clean manuel belge, form kayıt bildiriminden ayrı `transactional` billing sender/queue ve invoice-document idempotency anahtarıyla teslimata hazırlanıyor. Form kayıt endpoint’i `notification` kanalında kalıyor; sender profile message class ve workspace ile ayrılıyor. Güvenli app-origin belge bağlantısı varsayılan; provider URL, attachment benzeri kontrolsüz içerik, suppression, eksik recipient ve hazır olmayan belge fail-closed. Provider `accepted` durumu delivered sayılmıyor; mevcut outbox/retry/suppression sınırları korunuyor. Hedef manuel teslimat sözleşmesi ve ilgili email/invoice/replay regresyonları, TypeScript, production build, `/api/ready` geçti. Gerçek sender domain/provider teslimatı ve global R-10 `NO-GO/BLOCKED` korunuyor; otomatik/toplu fatura dispatch açılmadı.
- `V2-08A`: **LOCAL_PASS**. V2-08 teslimat sözleşmesi güncel release kararları altında yeniden doğrulandı; enqueue transaction’ı private/quarantined/clean document scope’unu tekrar kontrol ediyor, secure app-origin link ve transactional billing ayrımı korunuyor. Email/invoice/replay regresyonları, TypeScript, production build ve `/api/ready` geçti. R-10, Paraşüt ve otomatik/toplu dispatch durumu değişmedi.
- `V2-09`: **SUPERSEDED/BLOCKED**. İlk packet mevcut olmayan `tests/r10-final-gate.test.mjs` ve `tests/v2-payment-release-gate.test.mjs` dosyalarını zorunlu tuttuğu için workflow başlangıç kapısından geçmedi; ürün kodu veya önceki receipt boşa çıkarılmadı. Gerçek test adlarıyla `V2-09A` revizyonu hazırlandı.
- `V2-09A`: **LOCAL_PASS**. V2 first-party çıkış kapısı mevcut gerçek testlerle doğrulandı; local/staging teknik zinciri geçse de gerçek iyzico sandbox/merchant, muhasebeci manuel kabulü, AV/quarantine, sender domain ve staging test alıcısı dış kanıtları yok. Bu nedenle production/public release, live payment, SaaS ve otomatik faturalama açılmadı; global R-10 `NO-GO/BLOCKED` korunuyor.
- `V2-09B`: **LOCAL_PASS**. V2-09A first-party release karar receipt’i güncel karar kaydı, önceki V2 kapıları ve R-10 sınırları altında yeniden doğrulandı. Hedef release-gate testi, final/V1/V2/R-10 regresyonları, TypeScript, production build ve `/api/ready` geçti. Global R-10 `NO-GO/BLOCKED`; gerçek iyzico sandbox/merchant, manuel muhasebe kabulü, AV/quarantine, sender domain ve staging alıcısı dış kanıtları olmadan production/public release, live mutation ve SaaS açılmayacak.
- Sıradaki READY mikro-faz: `V3-00 — Paraşüt/ertelenmiş provider bağlantı ve OAuth kapsam kapısı`. Bu faz yalnızca ileride güvenli entegrasyon yapılabilmesi için provider-neutral bağlantı sözleşmesini ve resmi kanıt gereksinimlerini kilitleyecek; Paraşüt otomatik faturalama veya toplu dispatch açmayacak.
- `V3-00`: **LOCAL_PASS**. Paraşüt/ertelenmiş provider hazırlık kapısı OAuth authorization-code akışı, callback allowlist’i, tek kullanımlık workspace/user binding, provider company scope ve fail-closed health sonucu ile doğrulandı. Bu yalnız provider-neutral altyapı kanıtıdır; gerçek Paraşüt hesabı, token, GİB/mali müşavir onayı, live mutation ve otomatik fatura dispatch’i açılmadı. R-10 `NO-GO/BLOCKED` korunuyor.
- `V3-01`: **SUPERSEDED/BLOCKED**. Başlatma baseline’ından sonra kanonik yol haritasındaki V3-01 contact/product resolution hedefiyle hizalama gerektiği görüldü; credential/health taslağı V3-00 kapsamındaki kanıtı tekrar etmemek için receipt üretilmeden kapatıldı.
- `V3-01A`: **BLOCKED/SUPERSEDED**. Resolution testinde `tr-TR` locale lower-case dönüşümünün büyük `I` içeren aynı tax/email değerini dotless `ı` ile eşleştirmediği bulundu. Kaynak dosya mevcut packet’in allowed scope’unda olmadığı için güvenli olmayan kapsam genişletmesi yapılmadı; source düzeltmesi yeni packet’e ayrıldı.
- `V3-01B`: **BLOCKED/SUPERSEDED**. Contact düzeltmesi planlanırken product resolver’da da aynı locale bağımlı normalizasyon bulundu; mevcut packet product source’una izin vermediği için eksik kapsamla kısmi düzeltme uygulanmadı.
- Sıradaki READY mikro-faz: `V3-01C — Paraşüt contact/product resolution normalization fix`. Contact ve product resolver’larının locale bağımsız case-insensitive normalizasyonu ile güçlü/ambiguous/create kararları birlikte doğrulanacak; provider mutation ve otomatik faturalama kapsam dışıdır.
- `V3-01C`: **LOCAL_PASS**. Contact ve product resolver’larında locale bağımlı case-normalization hatası düzeltildi; VKN/e-posta/kod lookup, güçlü tek eşleşme, ambiguous/manual review ve explicit create preparation testlerle doğrulandı. Paraşüt provider mutation, otomatik fatura, document_ready ve toplu dispatch açılmadı; R-10 `NO-GO/BLOCKED` korunuyor.
- `V3-02`: **LOCAL_PASS**. Verified payment + approved invoice snapshot’tan Paraşüt satış faturası payload/command hazırlığı; contact/product mapping, minor-unit amount, currency/tax/date doğrulamaları ve request fingerprint ile doğrulandı. Gerçek provider POST’u, issued/document_ready, otomatik fatura ve toplu dispatch açılmadı; R-10 `NO-GO/BLOCKED` korunuyor.
- `V3-03`: **LOCAL_PASS**. e-Fatura/e-Arşiv tür ayrımı, bounded formalization job/polling, active document/PDF allowlist ve private/quarantine → document_ready koşulları testlerle doğrulandı. Gerçek GİB/Paraşüt formalization, canlı belge üretimi ve otomatik dispatch açılmadı; R-10 `NO-GO/BLOCKED` korunuyor.
- `V3-04`: **LOCAL_PASS**. Paraşüt health/error/limit/belirsizlik durumlarında manuel muhasebe fallback’i, stable-reference matching, güvenli invoice state geçişleri ve duplicate delivery/idempotency fence testlerle doğrulandı. Canlı provider mutation, otomatik fatura ve toplu dispatch açılmadı; R-10 `NO-GO/BLOCKED` korunuyor.
- `V3-05`: **LOCAL_PASS**. Otomatik Paraşüt faturalama çıkış kararı gerçek sandbox/test company, OAuth, e-belge, PDF, mali müşavir ve teslimat kanıtları olmadan `NO-GO/DEFERRED` olarak kilitlendi. Manuel fallback, ayrı billing sender, provider-neutral sınır ve SaaS tenant izolasyonu korundu; R-10 `NO-GO/BLOCKED` devam ediyor.
- `V4-00`: **HELD_BY_CONTEXT_BOOTSTRAP_GATE**. FORM-UX-07 geçti ancak `context-check` mevcut `STATUS.md` dosyasının 6 KB bootstrap sınırını aştığını bildiriyor. Geçmiş kayıtlar korunarak context bootstrap düzeltilmeden V4 tenant/veri sahipliği incelemesi başlatılmayacak; R-10 `NO-GO/BLOCKED` ve canlı SaaS/tahsilat yasakları ayrıca korunuyor.
- `FORM-UX-00..04A`: **LOCAL_PASS**. V4 öncesi UI/UX denetiminde uygulama shell/navigation, form seçili çalışma alanı, builder önizlemesi ve medya seçim/yükleme akışları doğrulandı; form medya endpoint'i global workspace medyasını kapsam dışı bıraktı. Her ilgili hedef test, TypeScript, production build ve readiness geçti. FORM-UX-04 packet revizyonu begin sonrası değiştiği için **SUPERSEDED/BLOCKED** kaydı olarak korunur; gerçek düzeltme 04A receipt'inde tamamlandı.
- `FORM-UX-05B`: **LOCAL_PASS**. App shell, Formlar çalışma alanı ve Builder köklerinde `min-w-0` shrink sınırı eklendi; responsive/a11y testleri, UI/layout regresyonları, TypeScript, production build ve readiness geçti. 05 ve 05A packet kayıtları kapsam/receipt kapısı nedeniyle **SUPERSEDED/BLOCKED** olarak korunur.
- `FORM-UX-06`: **LOCAL_PASS**. Public renderer ile embed script arasındaki submit postMessage sözleşmesi eşitlendi; WordPress iframe çıktısına güvenli referrer policy eklendi. Public/embed, WordPress ve medya sınırı regresyonları, TypeScript, production build ve readiness geçti. R-10 ve V4 kapıları değişmedi.
- `FORM-UX-07`: **LOCAL_PASS**. Tüm ana ekranların shell/view wiring'i, seçili form canlı form-istatistik-yanıt çalışma alanı, gerçek action/API bağlantıları, deferred kontrol dürüstlüğü, responsive/a11y ve public/private regresyonları birlikte doğrulandı. Final gate, TypeScript, production build ve readiness geçti; R-10 `NO-GO/BLOCKED` korunuyor.
- Sıradaki READY mikro-faz: `CONTEXT-00 — bootstrap/status context küçültme kapısı`. Geçmiş faz kayıtları kaybedilmeden kısa STATUS bootstrap ve tarihçe yönlendirmesi düzeltilecek; ardından V4-00 yeniden değerlendirilecek.

## Geçen yerel kapılar

- PAY ve manuel fatura sözleşme/test kapıları.
- Paraşüt P-00..P-11: OAuth, health, contact/product, sales invoice, e-Fatura/e-Arşiv job, bounded polling, active document/PDF.
- P-12A: enqueue transaction’ında gerçek invoice → payment → workspace/form/submission ve private/quarantined/clean document kapsamı.
- CTX-01B: kısa bootstrap, görev bazlı kaynak routing’i, IDE adapterları ve çalıştırılabilir 15 dakikalık workflow kapısı.
- Son görülen doğrulama: tam test runner 227 dosya, TypeScript, hedef lint, production build, `/` ve `/api/ready` başarılı.

## Açık kapılar

- P-12C-01..P-12C-06: document-ready karar sözleşmesi, kalıcı transaction, ortak delivery adapterı, authenticated server caller, ready route ve private retrieval yerel olarak geçti.
- `P-12C`: gerçek scan/match/approval sonucundan `document_ready` ve ortak enqueue bağlantısı.
- `P-12C-07`: kalıcı tenant/document karar modeli ve replay fence yerel olarak geçti; schema validate ve additive local migration deploy edildi. Ready akışının bu kaydı zorunlu kılması için `P-12C-08` gerekiyor.
- `P-12C-08`: ready transition/caller, yalnız kalıcı ve aynı workspace belge kararını kabul edecek şekilde bağlanmalı; request body tek başına kanıt olamaz.
- `P-12C-09`: authenticated approval endpoint ve server-side belge/adayı/workspace doğrulaması yerel olarak geçti; karar store'una yalnız stable match metadata'sı yazılıyor.
- `P-12C-10`: aynı belge için eşzamanlı approval istekleri unique karar çakışmasında yeniden okuyup duplicate/conflict olarak sonuçlanıyor; 500 veya ikinci karar üretmiyor.
- `P-12D-01`: ödeme handoff'unun succeeded/published snapshot koşulları ve document-ready → outbox zincirinin duplicate, gecikmiş belge ve cross-tenant fail-closed replay senaryoları yerel olarak geçti; gerçek provider/AV teslimatı hâlâ doğrulanmadı.
- `P-12D-02`: outbox worker lease, retry/backoff, dead-letter ve transactional/marketing suppression ayrımı yerel davranış testiyle geçti; gerçek provider teslimatı doğrulanmadı.
- `R-00-01`: verified refund/chargeback için fatura silmeden review/hold sözleşmesi yerel olarak geçti; gerçek provider refund/chargeback kanıtı ve kalıcı state mutation henüz yok.
- `R-00-02`: refund/chargeback hold geçişleri state graph'a eklendi ve workspace/invoice scoped compare-and-set transaction ile review state'ine alınıyor; provider event caller wiring eksik.
- `R-00-03`: verified payment state ile invoice refund-review persistence arasında server-side binding adapterı yerel olarak geçti; reducer/webhook/retrieve worker invocation wiring eksik.
- `R-00-04`: PASS. Retrieve reducer yalnız normalized ve verified refund/partial-refund/chargeback sonucunda invoice kaydını aynı payment transaction'ı içinde tenant-scope + CAS ile `refund_or_credit_note_review` durumuna taşıyor; audit metadata redacted kalıyor ve bloklanan handoff transaction'ı rollback'e zorluyor. Non-refund retrieve davranışı korunuyor. Hedef test, tam regresyon, TypeScript, production build ve `/api/ready` PASS. Provider refund/chargeback webhook caller wiring ve gerçek sandbox kanıtı hâlâ yok; aktif sonraki paket `R-00-05`.
- `R-00-05`: PASS. Webhook worker, yalnız imzası doğrulanmış ve payment transition tarafından kabul edilmiş Stripe `charge.refunded`/`charge.dispute.created` metadata'sını mevcut transaction-içi invoice refund handoff'una taşıyor. Tam/kısmi ayrım için provider status yoksa event fail-closed ignored kalıyor; mevcut inbox route'unda Stripe charge → payment_intent korelasyonu ve refund amount sınıflandırması henüz saklanmadığı için aktif sonraki paket `R-00-06`.
- `R-00-06`: PASS. Stripe webhook inbox route'u `charge.refunded` ve `charge.dispute.created` için canonical `payment_intent` kimliğini kullanıyor; charge ID ile payment order eşleştirmesi yapmıyor. `amount_refunded` yalnız toplam tutarla sınırlandırılmış geçerli değerlerde tam/kısmi refund sınıflandırıyor; eksik veya geçersiz korelasyon/sınıflandırma worker'da mutation'a girmiyor. Raw provider payload, secret ve kart verisi persist edilmiyor. Hedef test, tam regresyon, TypeScript, production build ve `/api/ready` PASS. Aktif sonraki paket `R-00-07`.
- `R-00-07`: İlk paket baseline kapsamı eksik olduğu için workflow tarafından kapatılmadı; değişiklikler tekrar edilmiyor, revizyon `R-00-07A` ile doğrulanıyor.
- `R-00-07A`: PASS. iYzico HPP webhook order eşleştirmesi Checkout Form’da saklanan token’ı, direct akış paymentId’yi canonical provider referansı olarak kullanıyor. Normalization yalnız doğrulanmış ödeme event allowlist’i ile `SUCCESS`/`FAILURE` durumlarını kabul ediyor; `REFUND`, `CANCEL`, `CHARGEBACK` ve bilinmeyen olay/status değerleri fail-closed kalıyor. Hedef test, 251 dosyalık tam regresyon, TypeScript, production build, `/api/ready` ve workflow verify PASS. Gerçek iYzico refund/cancel/chargeback merchant sözleşmesi veya sandbox kanıtı bu ortamda yok; bu nedenle bu mutation kapsamı açılmadı. Aktif sonraki paket `R-01`.
- `R-01`: PASS. Audit GET yalnız authenticated `audit.read` workspace scope içinde bounded kayıt okuyor; actor yalnız internal id ile dönüyor. before/after JSON, allowlist tabanlı redaction ile PII, e-posta, token, secret, belge içeriği ve bilinmeyen alanları dışarıda bırakıyor; bozuk legacy JSON route’u kırmıyor. Hedef audit/security testleri, 251 dosyalık tam regresyon, TypeScript, production build, `/api/ready` ve workflow verify PASS. Aktif sonraki paket `R-02`.
- `R-02`: PASS. Authenticated ve workspace-scoped invoice-center read model form, submission özeti, payment, invoice, document/ready ve delivery eksenlerini ayrı alanlarda birleştiriyor; stable cursor ve bounded filter kullanıyor. Recipient PII, raw document/provider payload, secret ve token DTO’ya alınmıyor; public route açılmıyor. Hedef read-model/PII testleri, 251 dosyalık tam regresyon, TypeScript, production build, `/api/ready` ve workflow verify PASS. Aktif sonraki paket `R-03`.
- `R-03`: PASS. Seçilen form çalışma alanına fatura merkezi eklendi; ödeme, invoice, belge ve teslimat eksenleri ayrı gösteriliyor. Gerçek `/api/invoices/export` ve `/api/invoices/import` endpoint’leri kullanılıyor; uygun olmayan/iade benzeri kayıtlar export seçimine giremiyor, server olmayan toplu gönderim disabled ve nedeni belirtiliyor. Hedef UI/action ve ilgili fatura testleri, 251 dosyalık tam regresyon, TypeScript, production build, `/api/ready` ve workflow verify PASS. Aktif sonraki paket `R-04`.
- `R-04`: İlk paket, mevcut olmayan `tests/role-ui.test.mjs` read kaydı nedeniyle workflow tarafından kapatılmadı; önceki R-03 kanıtını korumak için kaynak değişikliği bırakılmadı. Revizyon `R-04A` ile yalnız mevcut responsive toolbar yüzeyi doğrulanıyor.
- `R-04A`: PASS. R-04 revizyonu; yanıtlar arama alanına erişilebilir label eklendi ve action grubu dar ekranlarda `flex-wrap` ile taşmadan sarılıyor. R-03 receipt dosyaları korunarak hedef responsive test, 251 dosyalık tam regresyon, TypeScript, production build, `/api/ready` ve workflow verify geçti. Aktif sonraki paket `R-05`.
- `R-05`: PASS. Local manual-chain replay testi yalnız succeeded/paid ödeme adayını seçiyor; import replay tek kalıcı uygulama üretiyor; issued + approved + clean quarantine document-ready geçişi transactional e-posta komutuna bağlanıyor ve sabit idempotency anahtarı üretiyor. Pending/refunded ve document-ready olmayan durumlar fail-closed. Hedef zincir testleri, 251 dosyalık tam regresyon, TypeScript, production build, `/api/ready` ve workflow verify geçti. Gerçek Stripe/iyzico/Paraşüt, AV ve e-posta provider teslimatı dış bağımlılık olarak doğrulanmadı. Aktif sonraki paket `R-06`.
- `R-06`: PASS. Yerel Paraşüt v4 release-chain testi yalnız server-side sözleşmeleri birleştirdi: succeeded payment + paid_ready_for_invoicing payload, numeric company/job scope ve bounded async job, active document/PDF descriptor ve private app document URL üzerinden transactional delivery. Pending/refunded akış, provider URL/token/raw response sızıntısı ve document-ready öncesi teslimat reddedildi. Hedef Paraşüt contract/mapper/job/PDF/OAuth ve delivery testleri, 251 dosyalık tam regresyon, TypeScript, production build, `/api/ready` ve workflow verify geçti. Gerçek Paraşüt sandbox ve provider mutation dış bağımlılık olarak doğrulanmadı. Aktif sonraki paket `R-07`.
- `R-07`: BLOCKED. Güvenlik contract testi, invoice document retrieval sorgusunda explicit `visibility: 'private'` şartının bulunmadığını yakaladı. Route auth, workspace, readyAt, clean scan ve storage-root kontrollerine sahip; bulgu doğrudan public unauthenticated erişim kanıtı değil, private document invariant’ının defense-in-depth eksikliğidir. R-07A’da düzeltilecek; yüksek/kritik bulgu sayılmadığı için security fazı bu düzeltme doğrulanmadan ilerlemeyecek.
- `R-07A`: PASS. Invoice document retrieval route’una explicit `visibility: 'private'` filtresi eklendi; mevcut auth, workspace, readyAt, clean scan ve storage-root kapıları korundu. Hedef security/document route testleri, 251 dosyalık tam regresyon, TypeScript, production build, `/api/ready` ve workflow verify geçti. Aktif sonraki düzeltme `R-07B`.
- `R-07B`: PASS. Ready route request body'sinden recipientEmail, formTitle, invoiceNumber ve submissionId çıkarıldı. Authenticated server caller aynı workspace kapsamındaki InvoiceRecord → PaymentOrder → Form ve encrypted InvoiceRecipientSnapshot zincirinden form title, invoice number, submission ve alıcıyı türetiyor; e-posta yalnız server-side çözülüyor. Kalıcı bağ, workspace/submission eşleşmesi veya PII çözme başarısızsa outbox çağrılmadan fail-closed dönüyor. Hedef caller/route/security testleri, 258 dosyalık tam regresyon, TypeScript, production build, `/api/ready` ve workflow verify PASS. Gerçek provider/e-posta teslimatı hâlâ dış bağımlılık olarak doğrulanmadı. Aktif sonraki düzeltme `R-07C`.
- `R-07C`: PASS. Production session cookie `secure: true` olacak şekilde server-side `NODE_ENV` koşuluna bağlandı; yalnız local development HTTP istisnasında Secure kapalı kalıyor. Uygulama middleware'i production response'larında HSTS header'ı set ediyor ve static asset'leri hariç tutan kapsamla uygulama rotalarını koruyor. Session cookie/HSTS sözleşme testi, 259 dosyalık tam regresyon, TypeScript, production build, `/api/ready` ve workflow verify PASS. Gerçek production TLS/proxy/HSTS deployment kanıtı bu ortamda doğrulanmadı; aktif sonraki mikro-faz `R-08`.
- `R-08`: PASS. Outbox worker’ın claim sorgusu, worker çökmesi sonrası lease süresi dolmuş `sending` kayıtlarını yeniden recovery kapsamına alıyor; mevcut expired-lease CAS kontrolü ve invoice/document idempotency fence korunuyor. Yeniden claim CAS’ine status allowlist’i eklenerek tamamlanmış `sent` olayın yanlışlıkla diriltilmesi engellendi. Restart/restore runbook’u disposable backup, checksum, `DATABASE_URL`, `prisma migrate deploy`, `/api/ready`, claim/duplicate kanıtı ve production silme/reset yasağını içeriyor. Hedef recovery/worker testleri, 260 dosyalık tam regresyon, TypeScript, production build, `/api/ready` ve workflow verify PASS. Gerçek production backup/restore veya provider teslimatı doğrulanmadı; aktif sonraki mikro-faz `R-09`.
- `R-09`: PASS. Sentetik/anonymized mali müşavir kabul matrisi; TR şirket için doğrulanmış inbox sonucu ile e-Fatura/e-Arşiv ayrımı, bireysel/yurt dışı ve eksik kimlik için review, lookup hatası için fail-closed, TRY/USD minor-unit sınırı, iade/chargeback review ve issued → document_ready → delivery sırasını kanıtlıyor. Rapor, teknik testin hukuki/mali müşavir onayı olmadığını ve gerçek provider/GİB/production kanıtlarının dış bağımlılık olduğunu açıkça belirtiyor. Hedef kabul matrisi ve ilgili fatura testleri, 261 dosyalık tam regresyon, TypeScript, production build, `/api/ready` ve workflow verify PASS. Aktif sonraki mikro-faz `R-10`.
- `R-10`: NO-GO/BLOCKED. Yerel final gate testi, 262 dosyalık tam regresyon, TypeScript, production build ve `/api/ready` PASS; ancak gerçek Stripe/iyzico sandbox, Paraşüt/GİB ve mali müşavir/hukuk onayı, AV/quarantine, gerçek transactional teslimat, production TLS/staging ve backup/restore kanıtları doğrulanmadı. `RELEASE-DECISION.md` bu ayrımı kilitliyor; dış kanıtlar gelmeden faturalama veya release açılmayacak. Bu nedenle sonraki faza geçiş izni yok.
- `R-10A`: PASS. `docs/runbooks/r10-external-evidence-guide.md`, kullanıcı, ödeme sağlayıcısı, Paraşüt/GİB, mali müşavir, AV, e-posta ve deployment rollerinin hangi belgeyi sağlayacağını, hangi işlemi yapacağını ve hangi redacted kanıtın teslim edileceğini tek rehberde tanımlıyor. Secret, token, PAN/CVV, PII ve raw provider yanıtlarını yasaklayan sınırlar ile R-10 NO-GO koşulu korunuyor. Hedef rehber testi, 263 dosyalık tam regresyon, TypeScript, production build, `/api/ready` ve workflow verify PASS. R-10’un açılması için gerçek dış kanıtlar hâlâ bekleniyor.
- `R-10B`: PASS. R-10 dış kanıtları tamamlanmadığı için ödeme alma, fatura üretim/formalizasyonu ve fatura teslimatı canlı aktivasyonu açıkça `SUSPENDED_BY_R10` olarak kilitlendi. Form kayıt bildirimleri `notification`, fatura teslimatları `transactional` kanalı olarak; sender profile, template, queue/idempotency, suppression ve audit sınırları ayrı tanımlandı. Tek provider hesabı kullanımının izolasyonu bozmadığı, fatura sender’ının R-10 PASS + manuel muhasebe veya Paraşüt API v4 + document-ready kapılarına bağlı olduğu rehber ve testte doğrulandı. Hedef test, tam regresyon, TypeScript, production build, `/api/ready` ve workflow verify PASS. Bu plan kararı sonraki release/pilot fazını açmaz.
- `P-12D`: ödeme → belge → outbox zinciri replay ve gerçek sandbox kanıtı.
- Gerçek Stripe/iyzico/Paraşüt hesap çağrıları ve AV/provider teslimatı bu ortamda doğrulanmadı.
- Context kapısı `artifacts/workflow/CTX-01B/verified.json` içinde `LOCAL_PASS` olarak kaydedildi; `releaseVerified=false` ve bağımsız release onayı değildir.
- Build’te mevcut Next.js `middleware` → `proxy` convention uyarısı sürüyor.

## Çalışma sınırı

Yalnız `localhost:3000` MavenForms dev sunucusu gerektiğinde kontrollü kapatılıp açılabilir. Production/cloud durdurulmaz; migration reseti, veri silme ve broad cleanup yapılmaz. Ayrıntı: `AGENTS.md` ve `AI-RELEASE-EXECUTION-PLAN.md`.

## R-10C kapsam düzeltmesi — 2026-09-06

Global R-10 release kararı `NO-GO` olarak kalır; ancak bu karar first-party local/staging geliştirme capability’sini blanket hold ile durdurmaz. `R-10C` yalnız server-side merchant kimliği, gerçek provider sandbox kanıtı, manuel muhasebe yükleme/inceleme ve test alıcısı ile sınırlı pilot akışını tanımlar. Muhasebecinin dış sistemde kestiği fatura private/quarantine upload → eşleştirme → yetkili onay → `document_ready` → ayrı transactional billing sender zincirinden geçer. Production/live mutation, gerçek müşteriye teslimat, SaaS tenant adına ödeme ve SaaS abonelik tahsilatı kapalıdır. Kanonik sözleşme: `docs/runbooks/r10-first-party-pilot-and-manual-invoice.md`.

## 2026-09-07 anonim Sanal POS/Stripe araştırması karar güncellemesi

- Araştırma plan girdisi olarak kaydedildi: Türkiye first-party merchant için iyzico birincil adaydır; banka Sanal POS gerçek bir alternatif olmakla birlikte banka/merchant sözleşmesi, hosted/3DS, yabancı kart/kur, MAC-hash, retrieve/refund, IP/TLS ve reconciliation kanıtları sağlayıcıya göre değişir. Bu bulgular resmi aktivasyon veya canlı ödeme kanıtı değildir.
- V2 kapsamı iyzico-first olarak daraltıldı. V2 uluslararası ödeme taahhüdü vermez; banka Sanal POS, Stripe ve Google Pay direct `DEFERRED` adaylarıdır. Stripe için Türkiye merchant uygunluğu ayrıca kanıtlanmadan UI/provider mutation açılmaz.
- V2 manuel fatura yolu korunur: muhasebecinin dış sistemde kestiği belge private/quarantine → eşleştirme → yetkili onay → `document_ready` → ayrı billing sender ile yetkili manuel gönderim. Otomatik/toplu fatura gönderimi V2 gerekliliği değildir ve ertelenmiştir.
- Paraşüt otomatik faturalama silinmedi; V3’te risk, resmi API/GİB, mali müşavir, document-ready ve reconciliation kanıtlarına bağlı aday olarak tutuluyor. Erken fazlarda yalnız provider-neutral port/capability/evidence sözleşmeleri hazırlanabilir; Paraşüt’e özgü derin API uygulaması bu karar kapısından önce başlatılmayacak.
- Mevcut Stripe contract/conditional gate ve Paraşüt yerel sözleşmeleri boşa çıkmış sayılmaz; bunlar ileri entegrasyon sınırlarını ve fail-closed davranışı hazırlayan yerel altyapıdır, canlı entegrasyon kanıtı değildir.

## 2026-09-07 FORM-UX V4 öncesi denetim kapısı

- `FORM-UX-00`: **LOCAL_PASS**. Mevcut `/` ekranı canlı olarak incelendi; yanıt çalışma alanında seçili form için canlı yayın iframe'i, istatistikler, yanıt arama/filtre ve yanıt tablosu aynı akışta görünür. Route/action/API envanteri, mevcut UI regresyonları, TypeScript, production build ve readiness kapıları geçti. Bu yerel kanıt tek başına production release veya R-10 GO değildir.
- `FORM-UX-01`: **LOCAL_PASS**. Mobil menüde daha önce filtrelenen `Denetim` görünümü yeniden erişilebilir hale getirildi; sidebar/topbar/app-shell view wiring, stale form selection temizliği, UI regresyonları, TypeScript, production build ve readiness geçti. Bu düzeltme yalnız navigasyon kapsamındadır; diğer görsel-only veya davranış kanıtı bekleyen yüzeyler sonraki mikro-fazlara ayrıldı.
- `FORM-UX-02`: **LOCAL_PASS**. Formlar ekranındaki seçili yayınlanmış form çalışma alanına responsive canlı form iframe'i eklendi; form başına slug, sandbox ve yayın durumu korunuyor. Form listesi/kartı, istatistik/son yanıt, ayar action'ları ve fatura merkezi regresyonları, TypeScript, production build ve readiness geçti. Bu yerel kanıt production release veya R-10 GO değildir.
- `FORM-UX-03`: **LOCAL_PASS**. Builder Önizle action'ı gerçek authenticated draft preview iframe'ine bağlandı; alan/ayar/yayın action'ları ve unsupported yüzey ayrımı testlerle doğrulandı. Hedef wiring testi, UI regresyonları, TypeScript, production build ve readiness geçti. Bu yerel kanıt production release veya R-10 GO değildir.
- `FORM-UX-04`: **SUPERSEDED/BLOCKED**. Packet begin sonrasında kapsam/allowedFiles güncellendiği için workflow verify güvenli biçimde durdu; veri veya önceki kod geri alınmadı. Revize packet `FORM-UX-04A` ile yürütüldü.
- `FORM-UX-04A`: **LOCAL_PASS**. Ortak medya picker'ın medya klasörü seçimi, bilgisayardan yükleme, aktif thumbnail/metadata, alt metin, public imzalı URL ve form-scope sınırı doğrulandı. Form medya GET/POST route'u `scope=global` erişimini ve başka forma ait varlık bağlamayı reddediyor; hedef medya regresyonları, TypeScript, production build ve readiness geçti. Bu yerel kanıt production release veya R-10 GO değildir.
- `FORM-UX-05`: **SUPERSEDED/BLOCKED**. Hedef responsive düzeltmeleri doğru olsa da app-shell değişikliği mevcut packet'in allowedFiles kapsamı dışında kaldığı için verify yeni revizyona yönlendirdi.
- `FORM-UX-05A`: **SUPERSEDED/BLOCKED**. Revize packet begin baseline'ında in-scope değişiklik kalmadığı için verify güvenli biçimde durdu; yeni değişiklik yapılmadı.
- `FORM-UX-05B`: **LOCAL_PASS**. App shell/main, Formlar çalışma alanı ve Builder köklerinde flex shrink sınırı düzeltildi; Builder cihaz önizleme düğmelerine erişilebilir ad/pressed durumu eklendi. Hedef responsive/a11y, mevcut UI/layout regresyonları, TypeScript, production build ve readiness geçti. Bu yerel kanıt production release veya R-10 GO değildir.
- `FORM-UX-06`: **LOCAL_PASS**. Public renderer'ın submit olayı embed script'in dinlediği `submit` sözleşmesiyle eşitlendi; WordPress iframe çıktısına `strict-origin-when-cross-origin` referrer policy eklendi. Public/embed, WordPress ve medya regresyonları, TypeScript, production build ve readiness geçti. Bu yerel kanıt production release veya R-10 GO değildir.
- `FORM-UX-07`: **LOCAL_PASS**. Final UI/UX gate testi; tüm ana ekran wiring'i, canlı form/statistik/yanıt akışı, görünür actionların gerçek veya açık deferred durumu, responsive/a11y ve public/private sınırlarını doğruladı. Paket kapsamındaki tüm regresyonlar, TypeScript, build ve readiness geçti. R-10 `NO-GO/BLOCKED` devam ediyor.
- Sıradaki READY mikro-faz: `CONTEXT-00 — bootstrap/status context küçültme kapısı`. FORM-UX kapısı geçildi; ancak context-check düzelmeden V4 başlatılmayacak. V4 yalnız tenant izolasyonu için değerlendirilecek, canlı SaaS/tahsilat ve provider mutation açılmayacak.
- `FORM-UX` sonraki mikro-faz sırası: `FORM-UX-01` app shell ve navigasyon action wiring; `FORM-UX-02` formlar listesi/kartı ve form-seçim çalışma alanı; `FORM-UX-03` builder alan/ayar/önizleme/yayın bağlantıları; `FORM-UX-04` medya seçici-yükleme-scope ve aktif önizleme; `FORM-UX-05` responsive/a11y desktop-tablet-mobile; `FORM-UX-06` public form/embed/WordPress smoke; `FORM-UX-07` tam UI/UX regresyon ve V4 geçiş kararı.
- Her mikro-faz tek ölçülebilir çıktıdır ve 15 dakika ile sınırlıdır. UI'da görünen kontrol server action ve test kanıtı olmadan PASS sayılmaz; arada kalan veya görsel-only yüzeyler sonraki faza yönlendirilir.
- `V4-00` FORM-UX-07 sonrasında context bootstrap nedeniyle **HELD_BY_CONTEXT_BOOTSTRAP_GATE** durumundadır. Bu ürün işlevi blocker'ı değildir; geçmiş kayıtlar korunarak `STATUS.md` bootstrap küçültülünce yeniden değerlendirilecektir. R-10 `NO-GO/BLOCKED` nedeniyle tenant adına tahsilat, subscription billing, provider mutation ve gerçek SaaS açılmaz. Ana PAY → manuel fatura → Paraşüt/ertelenmiş provider → document-ready → transactional delivery → pilot → FORM-UX → SAAS sırası değişmemiştir.

## 2026-09-07 V4 ileri safha sırası düzeltmesi

- V4-00..02 yerel tenant sınırı, entitlement ve BYO provider hazırlığı olarak korunur; bunlar SaaS çıkışı değildir.
- V4-03 ve sonrası first-party V2/V3, transactional mail ve pilot kapıları tamamlanana kadar `DEFERRED_BY_PRODUCT_ORDER / PARKED` durumuna alınmıştır.
- Uygulama yönlendirmesi MAIL-00..07, V2 manuel fatura teslimatı ve first-party pilot kanıtlarına döndürülmüştür. R-10 `NO-GO/BLOCKED` ve ana faz sırası değişmemiştir.

## 2026-09-07 R-10 yatay kapı açıklaması

- R-10 yalnız V4 SaaS gereksinimi değildir; V1/V2/V3 için ortak güvenlik, release ve dış kanıt kapısıdır. V1 form/veri sınırları, V2 first-party ödeme ve manuel fatura, V3 ertelenmiş provider/otomasyon çalışmaları kendi R-10 kanıtlarını taşır.
- V4 bu ortak tabana tenant izolasyonu, BYO provider/muhasebe/mail bağlantısı, abonelik durumu ve sınırlı destek erişimi koşullarını ekler. Bu nedenle V4 yine en son safha olarak parkta kalır.

## 2026-09-07 mail araştırması kaynak kararı

- `Anonim_Form_Platformu_Mail_Yapilandirmasi_Uygulama_Arastirmasi.md`: **ACCEPTED_AS_RESEARCH_SOURCE**. Kaynak özeti ve SHA-256 `docs/research-sources/2026-09-07-mail-configuration-research-receipt.md` içinde sabitlendi; tam rapor context maliyetini artırmamak için projeye kopyalanmadı.
- Mail ana planındaki MAIL-00..08 başlıkları korunarak selector ID/server authorization, otomatik-manual-batch idempotency, provider-specific webhook auth, legal purpose/consent ayrımı ve default-off tracking düzeltmeleri işlendi.
- Bu araştırma yalnız mimari/plan doğrulamasıdır; provider hesabı, DNS, domain reputation, staging alıcısı, hukuki/mali onay veya gerçek teslimat kanıtı değildir. R-10 `NO-GO/BLOCKED` ve ana faz sırası değişmedi.

## 2026-09-11 R10-V4-43 final local evidence

- `R10-V4-43`: **LOCAL_PASS**. R10-V4-40/41/42 receipt’leri, kanıt sınıfları ve dış bağımlılıklar redacted final raporda birleştirildi. `LOCAL_PASS` sonuçları `PILOT_PASS` veya `RELEASE_PASS` olarak yorumlanmıyor; production mutation `false`, R-10 ve V4 canlı kapıları `NO-GO/BLOCKED` kalıyor.

## 2026-09-11 R10-V4-44 independent review preparation

- `R10-V4-44`: **LOCAL_PASS**. Bağımsız inceleme kapsamı; tenant sınırı, upload/SSRF, redaction, queue/replay, document, restore, provider, legal ve V4 kapılarıyla checklist olarak sabitlendi. Dış kanıt ve reviewer imzası yok; R-10/V4 canlı kararı `NO-GO/BLOCKED`, production mutation kapalı.

## 2026-09-15 R10-V4-45 research source normalization

- `R10-V4-45`: **LOCAL_PASS**. `Anonim_Form_Platformu_R10_Production_V4_Live_Readiness_Arastirma_Raporu.md` SHA-256 ile kaynak kaydına alındı. Raporun araştırma/plan girdisi olduğu; gerçek merchant/provider, production DNS/TLS/e-posta, AV/KMS, backup/restore, hukuk/muhasebe veya bağımsız review kanıtı olmadığı açıkça ayrıldı. Güvenli yerel devam alanları provider-neutral sözleşme, sentetik test, evidence registry/expiry, quarantine/document-ready, tenant scope ve wizard doğrulamalarıyla sınırlı tutuldu. R-10 production ve V4 live `NO-GO/BLOCKED`, `productionMutationAllowed=false` kaldı.
