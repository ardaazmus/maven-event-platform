---
applyTo: "src/**/*.{ts,tsx},tests/**/*.mjs,prisma/**/*,scripts/**/*.mjs"
---

Read `AGENTS.md` and the selected packet before editing. Validate tenant scope, input, idempotency, public/private boundary, and authorization at the server boundary. Run the packet checks; do not report UI or mock behavior as a working integration.
